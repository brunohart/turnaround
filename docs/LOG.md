# Log

Newest first. One entry per slot.

## Day 14 — 2026-09-20 — Backlog

**CI, found and fixed the same day.** CI had not been green since Day 7: the Day 8, 9 and 10 runs were cancelled at GitHub's six-hour limit and Day 11's failed after five and a half. One cause. `run.sh` solves the sixteen at 90 s a day; the hosted runner has two to four cores where the bench machine gives CP-SAT eight workers, and found no first grid in the time: `UNKNOWN`. A day with no grid leaves its share of the week's terms to the days after it, so Monday to Wednesday then said `INFEASIBLE`, `plan` exited 2 for the lot, and `run.sh` — which reads 2 as "the terms conflict on purpose" — re-planned the week with `--relax`, dropping The Iron Tide's week terms one slow solve at a time for hours, and still had no grid. That is ADR-002 broken by an exit code: terms dropped because the machine was slow. **Fixed:** out of time is exit **4**, never 2, for a day, a week, a festival and a what-if (`_exit_without_a_grid`); a week with any `UNKNOWN` day exits 4 and says that a later day's `INFEASIBLE` may be the clock's; `run.sh` stops on 4; a test holds both codes (the sixteen at 0.2 s is 4 and offers no `--relax`, the overbooked Regent is 2 and does). CI skips the sixteen's solve (`TURNAROUND_SKIP=sixteen`; a benchmark is not a proof and belongs to the bench machine) and still runs the checker over its committed grid; the job has a 40-minute timeout and a newer push cancels an older run. 140 tests.

Backlog-driven from today, and Linear is not reachable from this slot, so no issue could be taken; nothing scheduled. The day went to catching up Days 12 and 13 below.

## Day 13 — 2026-09-20 (scheduled Fri 19 Sep; slot did not fire) — The write-up

**Shipped.** `README.md` rewritten from Day 9's state to the whole fourteen days: the commands table gains `explain`, `what-if` and `diff`; sections with their sheets for the why marks (Day 7), the festival (Day 10), the re-plan (Day 11) and the board (Day 12); **At scale**, the benchmark table from `docs/bench.md` cut to six rows with the target written first and the sentence that it was not met; **The argument** — why constraint programming for the cinema week (the week is already written as constraints, and a penalty score will trade a distributor's minimum for forty admissions), why the checker (two readings that agree are evidence; the hand-made Thursday's five slips), and **what the tool refuses to do**, six refusals each with its ADR; a **Fourteen days** table, Day 0 to Day 13, every day linked to its screenshot; *What is still rough* in place of *What it does not do yet*; the proof block gains the board's two scripts and says CI runs the first three. `docs/post.md`, *The answer was no*: a 2,000-word draft, evidence first — the Day 1 `INFEASIBLE` message as the hero and how it is made (an assumption literal per term, each term tried alone, the core shrunk by deletion, said in the trade's words), the two decisions that followed (ADR-002, ADR-009), the test that guessed which minimal conflict and was wrong; the model in a paragraph; the checker and the five slips; the Day 8 miss with its table and the three things in it worth more than the number (two exact reformulations that did nothing or harm, the one inexact switch that helped most and is therefore stated on the sheet, the run-to-run noise); five things to tell someone starting; what it is not. Every number in it is in this log or in `bench.md`. `uv build` makes `turnaround-0.1.0.tar.gz` and the wheel, the five templates inside it. Tag `v0.1.0`. No code changed today.

**Solver.** Not run again; Day 12's regeneration an hour earlier stands (Regent `OPTIMAL` · 4.30 s · 1,608.6; the sixteen's week `FEASIBLE` · 660.2 s · 52,016.6; every grid checks). The floor: build clean, 139 tests green in 128 s.

**Broke and fixed.** The README's first draft gave `what-if` a `--without` flag that does not exist; it is `--drop`, and `--set key=value` for policy. The post's first draft called the checker "a few hundred lines"; it is 791 beside the solver's 1,269, and says so now. The hero caption still claimed 7.5 s from Day 3's model; about five.

**For Bruno.** PyPI: check `turnaround` is free on pypi.org first (it may well not be; `turnaround-grid` is the fallback and needs `name` in `pyproject.toml` and the README's install line changed), then `uv build && uv publish --token <pypi token>` from a clean checkout of `v0.1.0`. The post is a draft in a voice guessed from this log: cut freely, but check any number you add against `docs/LOG.md` and `docs/bench.md`. The Vercel deploy from Day 12 is still yours. Linear not reachable from this slot: *Day 12* and *Day 13* in BEDIP are still open.

**Still rough.** The README is long — the brief's reference section is most of it and belongs in `docs/brief.md` with a shorter front page. The hero image `docs/grids/regent.png` predates the why marks. The benchmark table in the README is pasted from `bench.md` by hand and will drift; the Fourteen days table links screenshots rather than showing them, which the playbook's "inline" half-asked for — seven are inline, the rest linked, because nineteen full-width images is not a front page. The post has no pictures and its claim that a programmer would rather be told *no* on a Wednesday is untested on any programmer. From tomorrow the slot is backlog-driven and needs Linear to be reachable to find any work at all.

## Day 12 — 2026-09-20 (scheduled Thu 18 Sep; slot did not fire) — The board

**Shipped.** `board/`: a static page that takes a grid JSON by drag-and-drop or a file input — a day, a week or a festival, and its brief beside it in either order — and draws the sheet in the browser. `board/sheet.js` is a port of `render.day_context` and the day macros of `_day.html.j2`: the ruler from the doors' hour to the last clear, the prime wash, a row per screen, the three-strip block (preshow on the ink hatch, feature in cream or white in prime, turnaround on the orange hatch pulled left over the credits), the why corners the grid already carries, the legend, the by-title table, and under 800 px the booth strips in place of the grid. A week or a festival is unfolded day by day in the browser (the day's policy, terms and screen overrides laid over the base; venues as screens) and drawn as day pages. A grid **without** a brief is drawn from what it says alone — screens and titles by id, runtimes from the feature's own minutes, no seats — and the bar above the sheet says *no brief*. The board **does not solve, check, count admissions, explain or diff**: the right-hand panel is stamped *The grid's claims*, prints status, objective, what was paid, what was given up and the probe's counts as the file states them, and says the proof is the checker's and the checker does not run in a browser (ADR-020). `board/sheet.css` is cut from the template's style macro by `render.board_css()` and written by `scripts/board.sh`, which also copies the Regent's brief and grid in as the page's one example; `tests/test_board.py` fails if the stylesheet drifts, if the example is not the Regent, or if any file in the board names an address, a storage API or a query string. `board/vercel.json`: clean URLs, `Content-Security-Policy: default-src 'none'` with only `'self'` opened, no referrer. `scripts/board.sh --serve` runs it on :8712 and `--preview` is the gated Vercel deploy. `scripts/board_pass.py` is the Playwright pass (the machine's own Chrome, no download): desk at 1280 and an emulated iPhone 13; every block on the board is held to the `left` and `width` the package's `regent.html` gives it, to a hundredth of a percent; a grid alone, a brief alone ("drop its grid as well"), a file that is not JSON, the week's seven day pages, the festival's ten, the example button; no page error, nothing scrolling sideways on the phone, and all 32 requests to the board's own origin. Screenshots `docs/grids/day-12-board.png`, `day-12-board-empty.png`, `day-12-board-festival.png`, `day-12-board-phone.png`, `day-12-board-phone-empty.png`. Three tests; 139 in all. ADR-020, the board paragraph in DESIGN.md.

**Solver.** Untouched; the regeneration's numbers. Regent: `OPTIMAL` · 4.30 s · 15 sessions · **1,608.6**. Booth: `OPTIMAL` · 2.86 s · 1,625.1. Regent overbooked: `INFEASIBLE`, then `--relax` drops the same four terms · `OPTIMAL` · 20.8 s · 1,573.2. Regent week: all `OPTIMAL` · 21.6 s · 106 sessions · 11,196.0 · 3 of 5 hold. Demand week: all `OPTIMAL` · 15.7 s · 106 sessions · 15,489.6 · 5 of 5 hold. Festival: all ten days `OPTIMAL` · 6.1 s · 74 screenings · 10,852.6. Sixteen: all seven `FEASIBLE` · 660.2 s · 545 sessions · **52,016.6** · Thu 7,405.4 gap 14.5 % · Fri 8,304.9 · 12.2 % · Sat 9,112.9 · 12.7 % · Sun 7,784.6 · 14.8 % · Mon 6,372.7 · 23.0 % · Tue 6,463.0 · 21.9 % · Wed 6,573.1 · 20.6 % · 6 of 22 hold — the best week yet and just over the band of Days 8–11 (51,223–51,827); the model did not change, so this is bench.md's wall-clock nondeterminism landing high, not a gain. Every grid passes the checker. `run.sh` ran in about 14 minutes.

**Broke and fixed.** The Playwright pass first counted three sessions of The Long Voyage; the Regent has four, and its grid is one of several equally optimal ones, so the pass now counts from the grid file instead of from memory. `inner_text()` returns the mono labels as the page shows them, uppercased, so the pass compares in lower case. `set_input_files` returns before the page has read the file; the pass waits for the error line instead of reading it at once. Python's `http.server` logged every request into the pass's output; a quiet handler. A request for `/favicon.ico` was the board's one 404; the page now carries an empty `data:` icon.

**For Bruno.** The deploy: `cd board && npx vercel` once to create the project as `turnaround` (root directory `board/`, no build command, no framework), then `scripts/board.sh --preview` for previews and `npx vercel --prod` for `turnaround.vercel.app`. Check the name is free first. Linear not reachable from this slot: BEDIP's *Day 12 — The board* is still open and wants the commit SHAs from `git log`.

**Still rough.** Two implementations of `day_context` now exist and the only thing holding them together is a Playwright pass that needs Chrome and is not in `scripts/test.sh` or CI; a Node-free check in CI would need the port to be testable without a browser, and it is not. The pass compares block positions on the Regent only; the booth's credits overlap, a screen with its own hours and the festival's fold were looked at, not asserted. The board draws less than the package's sheet: no *expected*, *turned away* or *sells*, no proof, no re-plan sheet, no terms sheets, no programme table across a week — a week is only its day pages, and the sixteen's seven pages are a long scroll with no way to jump to a day. The week's day is matched to the brief's by name and silently falls back to the first day when the name is missing. The header stamp still says *Week sheet* for a single day, as the package's does. Dropping a second grid onto a drawn sheet replaces the first without saying so. `what-if --diff`, Day 11's candidate for today, was not wanted by the board and was not built.

## Day 11 — 2026-09-17 — Diff

**Shipped.** `src/turnaround/diff.py` and `turnaround diff old.json new.json`. The diff reads two grids and nothing else — no brief, no solver, no checker (ADR-019) — and says what changed: sessions **added**, **removed** and **moved** (the same title at a new time, in a new room, both, or the same slot with a new *length* when the brief's runtime or turnaround changed under it), the show count per title before and after, the seats on offer when a brief is given (`--brief` also names the titles), and the grids' claims as a **terms delta**: status, objective, expected admissions, what was paid to hold and in clashes, and the terms each grid gave up (`now given up` / `held again`). Out as a rich table, as JSON on stdout (`--json`) or to a file (`--out`), and as the **re-plan sheet** (`--html`, needs `--brief`). A session is (screen, film, start, feature_start, feature_end, clear); the probe's `forced` is the solver's claim and not the session, and `stats` and `solve_seconds` are not a schedule, so a diff carries none of them. A title's unmatched sessions are **paired** by the matching whose starts moved least in total, a room change breaking a tie — exact up to seven a side, every matching tried; greedy beyond — so the Bee Kingdom's 12:30 went to 10:00 and its 17:00 to 14:35, which is how a person reads it, where closest-first had sent the 17:00 to 10:00. `invert(diff(a, b))` is `diff(b, a)` field for field and `apply(a, diff(a, b))` is `b`'s schedule and claims — the two promises the playbook asked for — and Hypothesis holds every random pair and triple of grids to both, plus composition through a third grid (`apply(apply(a, d_ab), d_bc)` is `c`); a diff of one day applied to another refuses in a sentence. A **week or a festival** is diffed day by day **by name** (`diff_week`), never by position: a re-plan that drops a day reads as that day all removed against an `ABSENT` grid, an added day all added, and `held` before and after rides the `WeekDiff`. **The sheet:** a moved block leaves a *ghost* where it was — a dashed ink outline on the fine ink hatch, nothing filled, the old start in mono, under the live blocks on the row it left (a room move ghosts the old room; a ghost past the ruler stretches the ruler) — an added block carries a small orange `ADDED` stamp at the corner of its feature, the one orange on the grid besides the turnaround and only on a re-plan sheet (DESIGN.md's Day 11 paragraph says why), and a removed session is struck through in rust at the end of its title's row in the by-title table, a moved one's old start struck in grey before its new one; a removed title with no session left today keeps its row even on a festival day whose fold would hide it (`day_context` keeps it; the test says so). A *Re-plan* stamp under the grid heads the table of every change — what, title, was, now — beside the claims in mono; the header says what the sheet is against; the booth strip carries the stamp and the old time; the legend explains the three marks. `render_html` takes `diff=`, `render_week_html` and `render_festival_html` take `diffs=` by day name, so `diff --html` on a week or a festival gives every day page its re-plan. `run.sh` now writes `docs/grids/regent-replan.{json,html}`: the Regent's Thursday as the manager typed it (Day 9) against the one the solver made — the Thursday re-plan artefact, screenshot `docs/grids/day-11-replan.png`. Seven tests in `test_diff.py`, one in `test_cli.py`, one in `test_render.py`; 136 in all. ADR-019.

**Solver.** Untouched today; the numbers are the regeneration's. Re-plan (hand-made → solved Regent): 1 added · 12 moved · 2 unchanged · sessions 14 → 15 · seats 2,744 → 2,840 · objective 1,532.3 → 1,608.6 (+76.3) · IMPORTED → OPTIMAL. Regent: `OPTIMAL` · 5.16 s · 15 sessions · **1,608.6** · probed 15 — another equally-optimal grid (10:40 not Day 10's 10:25), the same objective to the cent. Hand-made Regent: `IMPORTED` · 14 sessions · 1,532.3 by the checker's count · rejected on its five slips, as it should be. Booth: `OPTIMAL` · 2.71 s · 1,625.1. Regent overbooked: `INFEASIBLE`, then `--relax` drops the same four terms · `OPTIMAL` · 19.3 s · 1,573.2. Regent week: all `OPTIMAL` · 21.8 s · 106 sessions · 11,196.0 · 3 of 5 hold · Tue pays 120. Demand week: all `OPTIMAL` · 13.3 s · 106 sessions · 15,489.6 · 5 of 5 hold. Festival: all ten days `OPTIMAL` · 6.1 s · 74 screenings · **10,852.6** · nothing paid to clash. Sixteen: all seven `FEASIBLE` · 659.2 s · 545 sessions · **51,223.4** · Thu 7,386.7 gap 13.9 % · Fri 8,242.3 · 12.9 % · Sat 9,080.2 · 13.0 % · Sun 7,793.0 · 14.7 % · Mon 6,175.5 · 25.4 % · paid 1,140 · Tue 6,213.5 · 24.9 % · 1,140 · Wed 6,332.2 · 23.5 % · 1,020 · 3 of 22 hold — inside the band of Days 8–10 (51,229–51,827), bench.md's wall-clock nondeterminism. Every grid passes the checker. `run.sh` ran in about 14 minutes.

**Broke and fixed.** The first pairing was greedy, closest start first, and read the Bee Kingdom's two moves as 12:30 → 14:35 and 17:00 → 10:00 (a 125-minute move then a 420-minute one) where the shortest total is 12:30 → 10:00 and 17:00 → 14:35; the pairing is now the minimum-total matching, exact up to seven a side, with a cost and tie-break symmetric in old and new so the symmetry test still holds. The ghost block first carried the class `ghost`, which the sheet already uses for the misregistered title pass (orange, 9 %, absolute at 4px); the ghost is `was-here` now. A ghost's label — title, old start and where it went — did not fit a 50-minute box and overprinted the live block beside it; the ghost prints its old start alone and the hover title says the rest. Three lines over a hundred characters, once each in `diff.py`, `render.py` and the CLI test.

**Still rough.** A move is a reading, not a fact: the solver did not move anything, it made a new grid, and two grids five minutes apart everywhere read as twelve moves, which the Regent re-plan is — the ghosts of a five-minute nudge sit almost under the live block, and a threshold under which a nudge is "the same show, five minutes on" would read better than twelve ghosts and twelve rows. The exact pairing is factorial and stops at seven sessions a side; a sixteen-screen title with eight a day is paired greedily and may read differently from a person's reading. A diff across two different briefs (the what-if that dropped a title) names the dropped title by id where the new brief does not know it, and its removed rows cannot join a by-title table for a title the brief does not have. `what-if` still writes no diff of its own — `what-if --out` then `diff` is two commands where one would do. Nothing in a diff says why anything moved; `explain` still answers one grid at a time. The re-plan table on a phone is four narrow columns and the header's "against" line wraps. The week and festival re-plan sheets were rendered (the festival against itself: every day page says *no change*) but not looked at day by day.

## Day 10 — 2026-09-16 — The festival profile

*Run in two sittings. The first slot was cut off by a rate limit after writing the checker half — `FestivalBrief`, the festival terms on `Terms`, `Film.strand`, the two policies, `Grid.clash_paid`, the `print-move` and `strand-clash` checks and `check_festival` — uncommitted; the second read the diff, kept it (it followed ADR-003's order: checker first), tested it, committed it before touching the solver, and went on from there.*

**Shipped.** The second brief profile. A **`FestivalBrief`** — `festival`, `venues`, `films`, `policy`, up to 21 `days` — unfolds exactly as a week does into one plain `Brief` per day with the venues as screens, so `check`, `solve` and the day sheet never learn what a festival is (ADR-018). Three **festival terms**, which a day or a week brief refuses in a sentence (*"Gala: screenings 2 is a festival term and this brief is one day — put it in a festival brief"*): `screenings` (exactly this many across the festival), `guest` (a name, the days they are there, `earliest` and `latest`; `Sat 3 18:00–30:00` is how a window is written everywhere) and `available` (the days the print or DCP is in town, which unfolds to `max_shows 0` on the others and no candidates on them). Two **day policies**: `move_min`, the minutes one print needs between venues — the same title at another venue starts no sooner than the move after its feature ends, and never at the same minute — and `clash_penalty`, expected admissions an hour for two titles of one `strand` running at once at different venues, counted on the brief's start grid from doors to feature end, soft, the only soft term besides the week's hold. **The checker first:** `print-move` names each pair too tight (*Grand Hall ends 14:00, The Box starts 14:30*); `strand-clash` counts the overlap in grid minutes (`clashes()` names each: strand, minute, titles, minutes) and holds the grid to the penalty it says it paid, to the cent per grid step; `check_festival` counts `screenings` across the days (*2 of 2 · Fri 2 1 Sat 3 1*), finds the guest's screening inside their window on one of their days or says *no screening they can attend*, and refuses a screening on a day the print is away; a relaxation declared on any day covers the festival term; the objective row now reads admissions less hold less clash. **Then the solver:** `solve_festival` solves the days in turn like a week; `screenings` reaches a day as a floor (the count less what was screened, less what the days after could screen by `day_bound`) *and* a ceiling (the count less what was screened), the guest's screening as a debt on their last day with nothing delivered before it, and on the days before that one screening is kept back for them so the title cannot be used up before they arrive; each under its own assumption literal, so a conflict says *guest Sat 18:00–30:00 · owed today* and `--relax` gives up the guest alone. The print move is one exact clause per candidate (any two starts of a title at different venues within preshow + runtime + move conflict with each other, so `x + Σ others ≤ 1` loses nothing); the clash is an integer per (strand, grid minute) the objective pays for, counted on the brief's grid whatever grid the day is solved on; `Grid.clash_paid` is what the day paid. **The CLI** takes a festival brief everywhere a week goes (`plan`, `check`, `render`, `terms`, `export`, `validate`, `import --brief … --day`) and refuses `explain` and `what-if` on one. **The sheet:** `festival.html.j2` — the programme by title across the days (a time and a venue in mono per screening, the guest's screening under a navy rule, their days washed, the days the print is away dark, the screenings count against what was booked, the guest's window with the checker's tick), the festival's proof beside a day-by-day line, then every day's grid with the venues as rows, the day's clashes named in a mono line under it, and the by-title and proof tables folded to the titles in town that day (*31 titles whose print is away today, 41 checks, none screened* — the sheet's reading only; the grid JSON and `turnaround check` keep every row). A strand is a mono tag beside the title and on the booth strip and never a colour; it rides the block's hover title, not the block, where a 15-hour ruler leaves no room. The terms sheet gains the festival's scope and keeps to the days the print is in town. **`examples/festival.json`:** the Brightwater Film Festival — 40 fictional titles in six strands (ten in Competition, eight Documentary, six Midnight from 21:00, six Retrospective on 35mm prints that only the hall and the annexe can run, six New Voices, four Family before 16:00), three venues (a 640-seat Grand Hall, a 220-seat Annexe, a 90-seat Vault), ten days from Fri 2 to Sun 11 Oct with late Fridays and Saturdays and 13:00 doors on weekdays, 74 screenings at one a day per title, nine guests, 45′ print moves, 40 an hour for a clash, a 15-minute grid, no stagger (three buildings, no shared lobby). `run.sh` gives it 20 s a day. Eighteen tests across `test_festival` (the unfold, the refusals, a print that cannot make the move and one in two rooms at once, clash minutes on the grid, the grid held to its declared clash, screenings counted, a guest screening they cannot attend and the same given up, a screening without the print) and `test_festival_solve` (the tiny festival solves and the checker agrees to the cent, the clash is paid and not hidden and costs nothing at zero, an unplaceable guest names the festival term and `--relax` gives it up alone). ADR-018; a festival paragraph in DESIGN.md. Screenshot `docs/grids/day-10-festival.png`.

**Solver.** Festival: all ten days `OPTIMAL` · 6.2 s · 74 screenings · **10,852.6** · no two titles of a strand run at once, so nothing paid · Fri 2 0.46 s 1,480.1 · Sat 3 0.90 s 1,859.8 · Sun 4 0.42 s 1,092.8 · Mon 5 0.27 s 905.6 · Tue 6 0.28 s 990.7 · Wed 7 0.24 s 799.9 · Thu 8 0.16 s 510.0 · Fri 9 0.21 s 637.6 · Sat 10 0.64 s 1,646.2 · Sun 11 0.19 s 929.9 · about 1,000–2,300 candidates a day · 62 checks green a day and the festival's 96 green. Regent: `OPTIMAL` · 5.49 s · 15 sessions · **1,608.6** · first grid 0.1 s · probed 15: 1 forced by the terms, 14 by the objective. Booth: `OPTIMAL` · 3.49 s · 1,625.1. Regent overbooked: `INFEASIBLE`, then `--relax` drops the same four terms · `OPTIMAL` · 20.2 s · 1,573.2. Regent week: all `OPTIMAL` · 23.8 s · 106 sessions · 11,196.0 · 3 of 5 hold · Tue pays 120. Demand week: all `OPTIMAL` · 11.5 s · 106 sessions · 15,489.6 · 5 of 5 hold. Sixteen: all seven `FEASIBLE` · 659.9 s · 543 sessions · **51,500.2** · Thu 7,376.1 gap 14.8 % · Fri 8,255.9 · 12.7 % · Sat 9,038.7 · 13.4 % · Sun 7,644.0 · 16.9 % · Mon 6,224.4 · 24.3 % · paid 1,140 · Tue 6,469.4 · 21.3 % · 900 · Wed 6,491.7 · 21.1 % · 900 · 3 of 22 hold — between Day 8's 51,827.1 and Day 9's 51,229.2, the same wall-clock non-determinism bench.md records. The hand-made Regent is still rejected on its five slips. Every grid passes the checker.

**Broke and fixed.** The first regeneration of the festival put one screening on the closing Sunday: everything else had been taken by the first day of its window, because the festival is solved day by day and a title's first eligible day sells it as well as any (ADR-010's cost, felt on a closing night). `available` is the honest answer — a real festival knows when its prints arrive — and seven of the example's windows now open on the closing weekend, which at one a day makes a two-screening title play both days. The print-move check as first written let the same print start in two rooms at the same minute (`a.start < b.start`); it is `<=` now and names the pair once. The solver's first conflict for an unplaceable guest named `screenings` as well as `guest`, because the reserve kept a screening back on the very day the guest was owed; the reserve now applies only on the days before. The festival's `max_shows 0` unfold leaked onto the terms sheet as a booking (*1 · Tue 6 0 · Wed 7 0 …*) and into the by-title and proof tables of every day (thirty `0 ≤ 0` rows); the terms sheet keeps to the days in town and the day page folds the away titles into one line. A first commit of the checker half swallowed a collection error because `pytest | tail` reports tail's exit; it was amended before push. `pgrep -f scripts/run.sh` matched the poll loop's own command line and waited on itself for twenty minutes; the run had long finished.

**Still rough.** The days first in a title's window still take its screenings unless a window or a guest says otherwise, and the example is shaped around that; a festival brief has to say when prints arrive, and a spread across the festival (two screenings three days apart, a title's second showing after word of mouth) is not a term the tool has. The guest reserve is one screening kept back, whatever the guest's window, and not a proof that their day can hold it — a Saturday whose hall shuts at 17:00 finds out on Saturday. The clash penalty is one number for every strand; the example never pays it, because three venues and sparse days make it cheap to avoid, and a festival that has to pay it is not an example yet. No day is hinted from the last (nothing holds), `explain` and `what-if` take a day of a house, and a festival day cannot be probed. The programme table is wider than the page at ten days and scrolls in its box; the phone shows it the same way. `run.sh` is now about 14 minutes.

## Day 9 — 2026-09-16 (scheduled Mon 15 Sep; slot did not fire) — In and out

*Run a day late, in the same session that caught up Day 8. The solver was not touched; every grid was regenerated once for the proof and the sixteen landed on a different equally-good week.*

**Shipped.** `src/turnaround/inout.py` and two commands. **In:** `turnaround import --csv sessions.csv` reads the showtimes export any ticketing system can make — four columns, `screen`, `title`, `start`, `runtime`, in a few spellings (`auditorium`, `showtime`, `running time`…), everything else ignored — and writes a grid the checker reads. Against the house's brief (`--brief`, or `--brief week.json --day Fri`) every screen and title is resolved by id then by name, each session's feature and clear times are derived from the brief's preshow, credits and turnaround, and a runtime that differs from the brief's is said and the brief's used. Without a brief a **skeleton** is written beside the grid: the screens the CSV names (seats from the CSV or `--capacity`, formats from what plays there, a screen's own clean where the CSV states one), the titles it plays (id from a `film` column or a slug, runtime, format, rating, credits), the house open from the first start's hour to the last start, the preshow and turnaround the CSV states or the defaults — and each assumption printed as a sentence, the last one saying *no terms — write them in and check again*. The import never judges the grid: it prints the checker's count of green checks and the slips in a line, exits 0, and says `turnaround check` prints the proof (ADR-017). An imported grid is `IMPORTED`, `admissions` unclaimed, no `stats`, no `forced`; its `objective` is the checker's re-count and the sheet says *made by hand · imported, not solved* and *by the checker's count*. **Out:** `turnaround export brief.json grid.json [--ical] [--csv] [--json] [--out-dir]` — a calendar per screen (`<grid>.screen-<id>.ics`: an event per session from doors to clear, floating local time, the feature and turnaround in the description, lines folded at 75 octets, a show past midnight on the next date), a flat CSV for signage (`<grid>.sessions.csv`, eighteen columns: the four the import needs and the rest it reads when present — `film`, `format`, `rating`, `capacity`, `preshow`, `clean`, `credits`, `date`, `day`, `daypart`) and JSON for a website (`<grid>.sessions.json`: titles, then each day's screens and sessions in clock time with the minutes beside), no format named meaning all three; a week goes out as one CSV and one JSON of seven days and a calendar per screen across the week, and comes back a day at a time. `examples/regent-hand.csv`: the Regent's Thursday as a manager typed it, fourteen sessions in four columns, with five slips — Harvest Moon at 19:10 on Screen 2 not clear when Dead Signal starts at 21:30, Dead Signal at 17:40 and The Long Voyage at 17:45 five minutes apart, Dead Signal at 15:30 before its 16:00, Atlas of Small Rooms one show short and no prime start — and the checker names each; `docs/grids/regent-hand.html` is its sheet with the five red crosses, and `run.sh` imports it, renders it and reports the checker's rejection rather than obeying it. `run.sh` exports every example into `docs/grids/export/` (forty-three files, sixteen of them the Palladium's calendars) and now runs every command through one helper that unhides `.venv` first. README rows and a paragraph with the screenshot; ADR-017. Ten tests (`test_inout`): the Regent grid survives export → import → check with every verdict the same and the session list identical; our own export makes a skeleton that reproduces the Regent's seats, Screen 3's 15-minute clean and every film id, and passes the checker; the booth's credits and per-format preshows survive the skeleton and the one check the skeleton fails is the stagger it assumed; a four-column export in foreign spellings with a blank line and a `25:15` show gets the defaults and three assumption sentences; the hand-made Regent fails exactly the five checks; seven refusals in sentences; the week goes out as one CSV and Friday comes back equal, `--day` on a day-less CSV names the week's day; the calendar folds past midnight and refuses an undated grid; the JSON is the grid in clock time; the CLI imports, checks (exit 1), makes a skeleton, wants a day for a week brief, and exports five files or one. Screenshot `docs/grids/day-9-hand-made.png`.

**Solver.** Untouched today; the numbers are the regeneration's. Hand-made Regent: `IMPORTED` · 14 sessions · **1,532.3** by the checker's count · 14 of 19 checks green, five slips named. Regent: `OPTIMAL` · 4.57 s · **1,608.6** · 15 sessions · first grid 0.1 s · probed in 49.4 s: 1 forced by the terms, 14 by the objective — a different equally-optimal grid from Day 8's (10:40 not 10:25 for the first Long Voyage), the same objective to the cent. Booth: `OPTIMAL` · 2.57 s · 1,625.1. Regent overbooked: `INFEASIBLE`, then `--relax` drops the same four terms · `OPTIMAL` · 19.2 s · 1,573.2. Regent week: all `OPTIMAL` · 20.6 s · 106 sessions · 11,196.0 · Thu 4.48 s, Fri 5.62, Sat 5.36, Sun 2.24, Mon 0.67, Tue 1.22 (paid 120 to hold), Wed 0.84 · 3 of 5 hold — Day 7's number again, not Day 8's 11,206.7, for Day 8's reason (Thursday lands on one of several equal grids and the hold days follow it). Demand week: all `OPTIMAL` · 11.8 s · 106 sessions · 15,489.6 · 5 of 5 hold. The sixteen's week at 90 s a day: all seven `FEASIBLE` · 659.8 s · 545 sessions · **51,229.2** (Day 8: 51,827.1, 1.2 % apart, the wall-clock nondeterminism Day 8 named) · Thu 91.1 s · 7,261.0 · gap 16.2 % · first grid 11.9 s, Fri 8,204.9 · 13.3 % (77 hinted, first grid 24.4 s), Sat 8,993.6 · 13.9 %, Sun 7,743.1 · 15.2 % (57 of 83 hinted), Mon 6,272.0 · 23.8 % · paid 1,020, Tue 6,335.7 · 23.1 % · 960, Wed 6,418.9 · 22.1 % · 900 · 5 of 22 titles hold. Every grid passes the checker; the hand-made one fails it, as it should.

**Broke and fixed.** The full `run.sh` cannot run inside one 600-second call (thirteen minutes, eleven of them the sixteen), so it ran detached and was polled; a `uv run` from the polling shell re-hid `.venv` while the sixteen was solving, and the `terms` step after it died with *No module named turnaround* — `run.sh` unhid the venv before `plan` only. Every command now goes through a `tr()` helper that unhides first, and the sixteen's terms sheet and exports were made by hand from its grid. The regenerated Regent landed on a different equally-optimal grid, and the JSON export test had pinned the first start to 10:25; it reads the grid now. The solver's grid carries the checker's `objective` row (a claim held to its count) and an imported grid does not (nothing claimed), which the round-trip test now says in a comment rather than tripping on. Two attempts at patching `cli.py` and `run.sh` missed their anchors — ruff had reformatted the one, and the other's own helper line contained the string the patch asserted was gone.

**Still rough.** A ticketing export writes a late show as the next date and `01:15`; the import reads only the tool's own `25:15` on the day's clock, and that adapter (fold a next-date early-morning row back onto the day before) is not written. A skeleton assumes the default stagger and no staff cap, so the booth's own grid fails its skeleton's stagger check until the house edits the policy in — honest, and the note says so, but a `--stagger` flag would save the edit. `export` names its files after the grid and takes a directory, not a name per file. The calendar carries no time zone (floating time is right for one booth, wrong for a chain). The JSON is one shape guessed for a website nobody has built yet; Day 12's board is the first reader and may want it different. The skeleton's `open` is the first start's hour, which is the house's doors only by luck. The sheet's `IMPORTED` stamp is text on the existing stamp; the design was not revisited for a hand-made grid (no navy, no forced corners — correctly, since nothing was probed — but nothing marks the slips on the grid itself, only in the proof). `run.sh` at thirteen minutes is longer than one tool call allows, and the slot has to run it detached; splitting the sixteen's week into per-day calls, or a `run.sh --quick` that skips it, is the fix.

## Day 8 — 2026-09-16 (scheduled Sun 14 Sep; slot did not fire) — Scale

*Run two days late, forced from the Day 10 slot; Days 8 and 9 were missed. The target was written into `docs/bench.md` and committed before the first optimisation, as the playbook asked.*

**Shipped.** `examples/sixteen.json`: The Palladium, sixteen screens in six capacity classes (two 400-seat PLF rooms, three 260-seat 3D rooms, four of 180, four of 140, two of 90 with a quicker clean, a 60-seat room that opens at noon), twenty-two fictional titles with terms — an event title exclusive through Sunday with a week floor of 28 shows and 7 prime starts, a second week floor on Meridian, two 3D family titles that stop at 17:00 and 18:00, late-only horror, a one-print title confined to the small rooms — a stagger of three starts in ten minutes, six rooms clearing at once, and a full week with a late Friday and Saturday, a Sunday opening at 11:00 and a Tuesday override. `docs/bench.md`: the target, the number hit, Thursday step by step under every tuning, the Regent for scale, and why the things not done were not done. The measuring: `Grid.stats` on every grid — candidates, booleans, rank literals, constraints, the start grid solved on and the one asked for with the reason in a sentence, the first grid's second, the solver's bound and the gap — printed by the CLI as a dim line, and the sheet says *on a 10-minute grid* and *not proven best, gap 15.6%* where they apply. The containing, in `solve.Tuning`: rank literals per **capacity class** rather than per screen (exact; two rooms with the same seats sell the same); the rank count per (title, daypart) capped by what the **house-wide stagger** lets start (exact); **identical screens ordered by load** (`identical_screens`: same seats, formats, hours, clean, and no booking that names some of them; exact, any grid permutes into the order; off under a probe, which names a room); and the one switch that is not exact and says so, a **candidate cap** of 20,000 above which the day is solved on the smallest multiple of `slot_min` that fits, never past 30 minutes — the sixteen's 34,109 candidates on the 5-minute grid become 17,059 on a 10-minute one, and the checker still passes it because every start is on the 5-minute grid too. **Hints**: `solve_week` starts each day from the day before's grid, `plan --hint yesterday.json` does it for a re-plan, and a hint is a starting point and never a constraint; a hint is *complete* when every session of it is a candidate today, and a partial one (Sunday after a late Saturday) pins only what it names. The defaults are what the bench measured: capacity-class ranks only on a complete hint, load ordering off, the stagger cap on, the candidate cap on (`--max-candidates 0` keeps the brief's grid). `plan --probe-budget SECONDS` bounds `--why` and marks what it did not reach `unknown`, which is the Day 7 note's answer for a house where one solve per session is an afternoon. `scripts/bench.py` writes the step-by-step table; `scripts/run.sh sixteen` regenerates one example and gives the sixteen 90 s a day. ADR-016. Eight tests (`test_scale`): the tuned Regent matches the plain one to the cent, twins are found and ordered and a booking that names one twin breaks the group, grouped ranks share a literal per capacity and cost nothing, the cap coarsens and says so and the checker accepts it, a hint is taken and changes nothing, the week hints each day from the last, a budget of zero marks every session unknown. Screenshot `docs/grids/day-8-sixteen.png`.

**Solver.** The target was one day of the sixteen under 60 s to proven or a 2 % gap. **Number hit: 13.4 %** (Thursday, hinted, 10-minute grid: 7,410.8 against a bound of 8,561.9); **16.3 %** unhinted; the Day 7 model 21.6 %. The rank literals' share, measured first as Day 3 asked: 43 % of the sixteen's booleans (25,593 of 59,739), 60 % on the 10-minute grid, and 12 % on the Regent (171 of 1,439), so Day 3's 0.8 s → 4.4 s was the objective's search and not the literals. Presolve alone is 8–13 s on this model and it is what makes the search possible: with probing off the first grid slipped from 12 s to 57 s. Load ordering cost the first grid outright (UNKNOWN at 60 s, every run) because CP-SAT finds the twins itself (`max_lp_sym` in its log); capacity-class ranks unhinted found their first grid at 50 s, 61 s and never, in three runs; hinted they give the best grid and the best bound. The sixteen's week in `run.sh` at 90 s a day: all seven `FEASIBLE` · 660.3 s · 543 sessions · **51,827.1** · Thu 91.3 s · 7,312.4 · gap 15.6 % · first grid 11.7 s, Fri 8,262.6 · 12.7 % (76 sessions hinted, first grid 24.6 s), Sat 9,061.4 · 13.2 %, Sun 7,790.8 · 14.7 % (57 of Saturday's 84 hinted: the partial case), Mon 6,315.6 · 23.5 % · paid 1,020 to hold, Tue 6,493.6 · 21.5 % · 840, Wed 6,590.6 · 20.2 % · 780 · 5 of 22 titles hold · 52 checks green every day and the week's own checks green. Regent: `OPTIMAL` · 4.205 s · **1,608.6** · 15 sessions · first grid 0.1 s · probed in 50.0 s: 1 forced by the terms, 14 by the objective. Booth: `OPTIMAL` · 2.877 s · 1,625.1. Regent overbooked: `INFEASIBLE`, then `--relax` drops the same four terms · `OPTIMAL` · 19.6 s · 1,573.2. Regent week: all `OPTIMAL` · 19.3 s · 106 sessions · 11,206.7 · Thu 4.65 s, Fri 6.26, Sat 3.87, Sun 2.12, Mon 0.71, Tue 1.14, Wed 0.38 · 3 of 5 hold — 10.7 better than Day 7's 11,196.0 because Thursday landed on a different equally-optimal grid and the hold days follow it more cheaply, the same ADR-015 cost as before, and the hint from the day before is why Monday to Wednesday now take under a second. Demand week: all `OPTIMAL` · 14.4 s · 106 sessions · 15,489.6 · 5 of 5 hold. Every grid passes the checker.

**Broke and fixed.** The sixteen's first week terms (36 shows and 12 prime for the event title) left Wednesday owing more than two PLF rooms can play; the owed floor made it `INFEASIBLE`, and the conflict search on a 60-second day — one feasibility solve per term for forty-odd terms, then the deletion pass — ran past twenty minutes before it was killed; the floors are now at the Regent's proportions (ADR-013's known cost, felt at scale). Sunday found no grid in 60 s on the first regeneration: it opens at 11:00 after a Saturday that ran to 23:00, so Saturday's grid was a poor hint, the hint was pinning every non-match to zero, and the rule was also switching to capacity-class ranks on it; a partial hint now pins only its matches and the class ranks wait for a complete one, and the sixteen gets 90 s a day. `uv` re-hides `.venv` under `~/Documents` between commands, not just at sync, so `run.sh` unhides it before every command. Ruff refused `Tuning()` as a default argument; `TUNED` is the module-level singleton. The `Tuning` insert once landed between `Owed` and its docstring.

**Still rough.** The target was not met, and the bound is the loose half of the gap (the LP relaxation of the rank literals lets fractional sessions sell); a per-daypart capacity cut, or solving the ranks' LP first, is the next lever and the sixteen's Thursday is the test. Monday to Wednesday pay 780–1,020 to hold on the sixteen — thirteen to seventeen titles differ from Thursday — because a `FEASIBLE` grid at 90 s has not found the holding grid, not because it does not exist, and the sheet's *hold* column is honest about it and nothing more. No start is dominated in the exact sense on this model (bench.md says why) so nothing was pruned; a heuristic prune would be a hidden relaxation. The stagger cap on the ranks never bites on the sixteen (three starts in ten minutes is generous); it would on a one-in-ten house of small rooms, and that house is not an example. The candidate cap and the 30-minute ceiling are numbers picked for one house on one machine. CP-SAT is not deterministic under a wall-clock limit with eight workers: the same model's first grid moved from 15 s to 50 s between runs, and every number in bench.md moves a few percent. Two measurements Day 4 and Day 5 left for today were not taken — the staff cumulative's share of solve time and `day_bound`'s cost of re-enumerating candidates per film per day (twenty-two titles × seven days × 34,109 candidates on the sixteen, perhaps eight seconds a week) — and are moved to Day 13 with the bench table. A full probe of the sixteen is off the table (about seventy 90-second solves); `--probe-budget` is the answer and it has not been run on the sixteen. The sixteen's terms sheet and week sheet are long (543 sessions) and were looked at only at the top. `run.sh` now takes about 13 minutes, eleven of them the sixteen.

## Day 7 — 2026-09-13 — Explain

*Run in the same session as Day 6, straight after it. The slot's agent was stopped part-way by an unanswered permission prompt with the code written and uncommitted; the day was finished by hand from `git status`.*

**Shipped.** Every session has a *why*, and the two halves of it are kept apart (ADR-015). The checker's half, `check.whys`: the terms the session helps satisfy (`min_shows 4/3`, `prime_shows 1/1`, `exclusive_screen 1`, `latest_start ≤ 17:00`), its daypart and rank, what it sells and what it turns away — re-derived from the brief and the grid with the same `ranked` arithmetic `admissions` now uses, so a title's whys sum to its admissions row and the test says so. The solver's half, `solve.probe`: forbid the *show* — this title, in this room, in this daypart (`show_of`), because forbidding one five-minute start only nudges it — and solve the day again with whatever the grid already relaxed still relaxed. `INFEASIBLE` is `forced by the terms` with the conflict named; a proven grid that sells fewer seats is `forced by the objective · −121 without it · the slot goes dark`; an equally good grid is `free`; a timed-out probe is `unknown`, and a worse-but-unproven grid is `unknown` with the delta as an upper bound. `Forced` lives on the session, round-trips through the grid JSON, and the checker ignores it. `turnaround explain brief grid --session 1@17:30` prints one session's why and probes it; without `--session` it probes them all and writes them back with `--out`. `turnaround what-if brief --drop bees` (or `--set max_concurrent_turnarounds=3`) re-solves the day without a title or under a changed policy, never relaxing a term, and prints the two days side by side: shows and expected admissions before and after, new starts in blue, gone starts struck, where each freed slot went, the objective's delta. `plan --why` probes every session; `run.sh` does that for the Regent only. The sheet: a navy corner on a feature block the terms force, an ink corner at 45% on one the objective forces (the orange stays on the turnaround, DESIGN.md), both in the legend; the by-title table's starts column became *starts · sells · forced* (`17:45 220 FORCED`, `10:10 121 −121`, a short sale in rust); the proof table gained a *Forced* column naming the sessions each term forces, and for the objective row how many and the three that cost the most; a mono line under the proof says `15 sessions probed · 1 forced by the terms · 14 by the objective · 0 free — the solver's claim, not the checker's`; the hover title on every block carries the why. Ninety-seven tests (twelve new: `test_explain`, `test_cli`). Screenshots `docs/grids/day-7-why.png` and `day-7-why-full.png`.

**Solver.** Regent: `OPTIMAL` · 5.728 s · **1,608.6** · 15 sessions · 14 turned away; probed in 58.9 s: the 17:45 Long Voyage is forced by `odyssey prime_shows` (1.1 s to prove), every other session is forced by the objective — the 10:10 Long Voyage by 121 (without it the room goes dark and the title plays three), the 14:05 by 92 (it shifts to 13:50), the 21:15 by 70 (to 20:40), the Bee King's two by 29 and 23, and the rest by 1.5 to 20 as they move to the other room ten minutes off — and none is free. Booth: `OPTIMAL` · 4.099 s · 1,625.1 · 15 sessions. Regent overbooked: `INFEASIBLE` then `--relax` drops four terms as before · `OPTIMAL` · 20.5 s · 1,573.2. Regent week: all seven `OPTIMAL` · 23.1 s · 106 sessions · **11,196.0** · Thu 6.47 s · 1,608.6, Fri 7.29 s · 1,681.2, Sat 3.69 s · 1,681.2, Sun 2.65 s · 1,552.7, Mon 0.49 s · 1,608.6, Tue 1.74 s · 1,455.2, Wed 0.64 s · 1,608.6 · 3 of 5 hold. Demand week: all `OPTIMAL` · 11.9 s · 106 sessions · 15,489.6, all five titles hold. The checker passes every grid. The regenerated Regent is a different optimal grid from the morning's (10:10 where it had 10:35, the Bee King at 10:00 and 17:00 where it had 12:25 and 14:30), same objective to the tenth; the probe's answers are for the grid it was given, which is ADR-015's last cost.

**Broke and fixed.** The proof table's objective row listed all fourteen sessions the objective forces in one cell, nowrap, and the panel clipped it at "1@14:15 −"; the row now says how many and names the three biggest, and the term rows stack one session per line. The objective-forced corner was orange, and DESIGN.md says the orange is the turnaround and nowhere else on the grid; it is ink at 45% now. `TermRef` moved above `Forced` in `model.py` because `Forced` carries a list of them and pydantic wants the name bound first.

**Still rough.** A full probe is one solve per session, a minute for the Regent; a sixteen-screen day (Day 8) cannot afford it without a budget, and `unknown` will appear in the counts as soon as the limit bites. Only a day is probed; `explain` on a week means picking a day of it, and a what-if for the week — Day 5's "spread the 28 over the week" — is not built: `what_if` takes a day brief. The `instead` clause is a heuristic reading of the grid without the show (same title moved, same room taken, slot dark) and can misname a reshuffle that touched three rooms; it says "moves to Screen 3 10:00" for Harvest Moon when the whole morning was re-dealt. The what-if prints the day beside the day; it does not write a diff artefact, which is Day 11. The whys name day terms and week terms by count only (`min_shows_per_week 28 · 4 today`) without the debt the day owed. The hover title is the only place the why reaches the printed strip, and print has no hover. Free sessions (`by: free`) have not been seen on any example yet, so the `free` branch of the sheet is exercised by the tests alone.

## Day 6 — 2026-09-12 — The sheet, properly

*Run on 2026-09-13, the morning after: the Day 6 slot did not fire on the 12th, so Days 6 and 7 ran back to back.*

**Shipped.** The identity pass, against `digital-design-taste.md` line by line. The feature block's second orange pass was there since Day 0 and never visible — `.feat` clipped its own overflow and the pseudo-element sat behind the lane in the root stacking context; it now has its own stacking context and clips only its text, so every block on the grid reads as a print that is 5px off register. Rubber stamps carry the pad's navy offset behind them (3px, 18%) and the sheet's background under them. A second colour field, navy, bleeds off the bottom-left behind the by-title table beside the orange one off the top-right, and `body` hides the horizontal overflow they cause. The meta block is pinned at +0.4°, the legend at −0.3°, the two panels below the fold at −0.3° and +0.35° and each straightens on hover, as the taste says a card does; the grid itself stays level, because a block's position is data. The ghost pass is one rule (`.ghost`) and sits behind every day and screen heading, not only the house. The broken divider, the grain and the misregistered `h1` were already right and were left alone. **Print, from the same HTML.** `@page pinup` is A3 landscape and `.sheet` is on it; `@page strip` is A4 portrait and every `.strip-page` is on it, so Chrome breaks between them on the name change alone. The booth strip is the three-strip block turned vertical: doors and the preshow's minutes on the ink hatch, the feature with its start–end and runtime, the turnaround on the orange hatch with when it begins (*over the credits* where it does) and when the room is *clear*, and between sessions the dark minutes or *back to back*; one page per screen, the screen's name in poster type with its own doors and last start under it. Backgrounds are forced with `print-color-adjust`, tables may break across pages (rows may not), and every page-opening section carries 3px of air at the top. `render.py` gives each block its minutes (`pre_min`, `clean_min`, `over_min`, `gap`) and each row its `open`, `last_start` and `clean_min`. The terms sheet is on its own `@page letter`, A4 portrait, one title per page with the footer on the last. **The phone.** Under 800px the grid is hidden and the strips take its place, ordered before the tables with flex `order`; the by-title, proof and week tables scroll sideways inside `.scroll`; the header and panels stack. The phone rule is `@media screen and (max-width:800px)` because Chrome's print layout evaluated the unscoped one as true and printed the phone view. **`scripts/shot.py`.** Headless Chrome will not lay a window out narrower than about 500px, so `--window-size=390` was a crop of a wider page, not a phone; the script drives Chrome over the DevTools protocol with `Emulation.setDeviceMetricsOverride`, captures the viewport or the full page, and needs only `websocket-client` (inline script metadata, `uv run scripts/shot.py`). Five tests in `tests/test_render.py`: one strip page per screen with the turnaround called out and the credits overlap named; the three page names declared in the HTML and the phone rule scoped to screen; every identity mark present; the week sheet carrying strips for every day and screen; the terms sheet a letter. 85 tests. The README's hero `regent.png` regenerated under the pass.

**Solver.** Booth: `OPTIMAL` · 2.603 s · **1,625.1** · 15 sessions · 16 turned away. Regent: `OPTIMAL` · 7.521 s · 1,608.6 · 15 sessions. Regent week: all `OPTIMAL` · 28.9 s · 106 sessions · **11,196.1** · Thu 6.04 s · 1,608.6, Fri 5.67 s · 1,681.2, Sat 11.03 s · 1,681.2, Sun 2.99 s · 1,552.7, Mon 0.89 s · 1,608.6, Tue 1.28 s · 1,455.2, Wed 0.81 s · 1,608.6 · 3 of 5 hold · Tuesday pays 120 — Day 4's value again, since Thursday's anchor tie fell on 10:30 this run and 10:40 on Day 5; the week has several optimal grids and the log will keep saying so. Demand week: all `OPTIMAL` · 19.3 s · 15,489.6 · 5 of 5 hold. Regent overbooked with `--relax`: `OPTIMAL` · 22.8 s · 1,573.2 · the same 4 terms dropped. `run.sh` 90 s; every example checks green. Screenshots: `docs/grids/day-6-sheet.png` (the booth at 1280), `docs/grids/day-6-phone.png` (390 px, the full page, real device emulation), `docs/grids/day-6-print-a3.png` (page 1 of the print: the A3 pin-up, grid and both tables on one sheet, the credits-overlap hatch legible at that size — Day 4's question answered), `docs/grids/day-6-print-strip.png` (page 2: Screen 1's A4 strip). The print of the Regent week is 29 pages: one A3 front, then per day one A3 and three A4.

**Broke and fixed.** The navy wash, first child of `.cols`, took the first grid cell and pushed the by-title table into the proof's column: `.cols > div { position:relative }` outranked `.wash`'s absolute, so the panels are now `.panel` and the wash is left alone. `text-overflow:ellipsis` on the feature label turned "Dead Signal 21:30 · 92′" into "21:30 · 9…" — the block clips like a print again, it does not abbreviate. The strip's ghost pass rendered inline ("Screen 1Screen 1") because the rule named only `h1` and `h2`. The first print came out as the phone view (grid hidden, strips first, header stacked) because the unscoped `max-width:800px` query matched Chrome's 800px print window. The proof table, unbreakable, jumped to a near-empty second A3; tables may break now and the pin-up is one page. A 1px ink wedge at the foot of every printed page turned out to be the next page's rubber stamp: rotated −0.8°, its right end rose half a pixel above the page top and Blink painted that sliver at the bottom of the page before. The terms footer landed alone on a sixth page because the print rule put `footer` on the strip page and a letter has no strips. The two-day week fixture refused itself: `exclusive_until Sun` on a week with only Thu and Fri, exactly as Day 5 said it would, so the fixture drops the week terms. The A3 PDF is 33 MB — Skia rasterises the repeating-gradient hatches — and is not committed; the page PNGs are.

**For Bruno.** Nothing.

**Still rough.** The printed PDF is heavy (33 MB for four pages) because the hatches are CSS gradients; SVG pattern fills under `@media print` would fix it and are a Day 12 concern when the board ports the block. The week's strips are one A4 per screen per day — 21 for the Regent — and a small house would rather pin one page per screen for the week; the day strip is the right unit for a multiplex, so this is a second layout, not a fix. The phone's by-title week table is a sideways scroll, which is honest but not designed. Rotating the panels blurs table text a fraction in Chrome (subpixel), which is the taste's rough edge and may be reconsidered for the proof. Long titles still clip at the block edge on the desktop grid ("The Bee King"); the strip and the hover title carry the full name. The sheet links no fonts: on a machine without Space Grotesk, Inter and JetBrains Mono it falls to Helvetica and Menlo, and whether a document meant to be pinned in a booth should fetch from Google Fonts is a decision not yet taken. `test_render` solves the booth once (cached) and a two-day week and still costs 40 of the suite's 73 s. `shot.py` hard-codes the Chrome path and is macOS-only. Print previews are rasterised with `pypdfium2` outside the project — `uv run --no-project --with pypdfium2 --with pillow` — which is fine for the log and wrong for CI.

## Day 5 — 2026-09-11 — Distributor terms, fully

**Shipped.** The booking is one document, so the new terms live in `Terms` beside the old, each with a scope (`TERM_SCOPE`). *`plf_lock`* (a day term): no other title on any room whose formats include PLF, a dark PLF room allowed; refused at validation where the house has no PLF room. *`exclusive_until`* (a week term): a day name; `week.day(i)` sets `exclusive_screen` on every day through it and a day override still wins, so the Regent week's three Mon/Tue/Wed overrides became one line. *`min_shows_per_week`* and *`prime_shows_per_week`* (week terms): the week is still solved day by day (ADR-010), and a week term reaches a day as what it is *owed* — the term less what the days before delivered less the most the days after could (`day_bound`: blocks that fit between a title's first and last allowed start per screen, capped by `max_shows`); the day enforces that floor under an assumption literal whose name says so (`min_shows_per_week 40 · 32 owed today`), a conflict prints it, `--relax` drops it for that day only, and a week term that asks nothing of today is not in the model at all (ADR-013). A day brief refuses a week term in a sentence. The checker was written first: `plf_lock` per day; `min_shows_per_week`, `prime_shows_per_week` and `exclusive_until` at week scope, re-counted from the sessions alone, a shortfall accepted only where some day's grid declares the term given up, and the week checks now carry the film so the CLI and the sheet name the title. Validation speaks the trade's language: `validation_sentences` walks every pydantic error to the title or screen it belongs to and says what the booking can carry (*"The Long Voyage → terms: `min_show` is not a term a booking can carry — the fields are min_shows, max_shows, …"*), impossible windows and counts are refused before the solver sees them (*"Dead Signal: earliest_start 16:00 is after latest_start 15:00 — no session could start"*, *"Atlas of Small Rooms: min_shows 2 is more than max_shows 1"*, *"latest_start 09:00 is before the house opens at 10:00"*), a PLF lock in a house with no PLF room says which formats the screens do play, and `exclusive_until Sat` on a week without a Saturday lists the days it has; `plan`, `check`, `render`, `terms` and `validate` all refuse a bad brief with those sentences and exit 1. The terms sheet: `turnaround terms brief grid --html` writes one page per title, mono on paper — every term, its scope (each day / the week), what was booked (with any day override beside it), what the grid delivered day by day, and a verdict that is the checker's, not the solver's: ✓ honoured in navy, *given up* as a rust stamp with the days, ✗ not met; then the sessions as delivered with the prime starts underlined in navy, and a stamp per title (*every term honoured* / *2 given up*). `run.sh` writes one per example. `examples/regent-week.json`: the opening title is booked for real — exclusive through Sunday, 28 shows and 7 prime starts across the week; `examples/demand.json`: the holiday event title holds the PLF room for the run (`plf_lock`) and the week has no exclusive overrides at all. 19 new tests in `tests/test_terms.py`: a hand grid with another title on the locked PLF room fails `plf_lock`; a forged week short of its minimums fails `min_shows_per_week` and `prime_shows_per_week` and passes unclean once a day declares them; a title handed onto the owned screen before the exclusive lifts fails `exclusive_until`; the solver meets a week minimum and a week prime minimum the days alone would not; a week minimum the week cannot carry names itself with what was owed and relaxes on that day; each impossible term is refused with its sentence; unknown fields are named by title; day terms relax before week terms; the terms sheet states every term, scope and verdict. The property strategy learned that `max_shows` below `min_shows` is now refused. 80 tests in 26 s.

**Solver.** Regent week: all `OPTIMAL` · 26.2 s · 106 sessions · **11,206.7** · Voyage 28/28 shows, 7/7 prime, own screen Thu–Sun · Tuesday pays 120 · 3 of 5 hold · every day 19–20 checks green and the week's 6. Per day: Thu 5.81 s · 1,608.6, Fri 11.49 s · 1,681.2, Sat 3.72 s · 1,681.2, Sun 2.67 s · 1,552.7, Mon 0.58 s · 1,608.6, Tue 0.99 s · 1,466.0, Wed 0.71 s · 1,608.6. Day 4's week was 11,196.0: Thursday has several optimal grids and the anchor tie broke differently (10:40 rather than 10:30), so Tuesday, still paying 120, lands 10.8 higher; every other day is the same value. Demand week with the PLF lock: all `OPTIMAL` · 15.0 s · 106 sessions · 15,489.6 · 5 of 5 hold · 0 paid · Screen 1 plays only Voyage all seven days, the same objective as Day 4 because the solver was already leaving it to Voyage. Regent: `OPTIMAL` · 6.836 s · 1,608.6 · 15 sessions. Booth: `OPTIMAL` · 2.888 s · 1,625.1 · 15 sessions. Regent overbooked with `--relax`: `OPTIMAL` · 21.0 s · 1,573.2 · the same 4 terms dropped. Screenshot `docs/grids/day-5-terms.png` (the Regent week's terms sheets, Voyage and Harvest above the fold). `run.sh` 81 s.

**Broke and fixed.** `.venv` went hidden again after `uv sync` and `turnaround` vanished from the shell; the scripts clear it, a bare `uv run` does not. Three forged-grid tests failed for the checker being right about more than I asked: removing a title's sessions on hold days also breaks `held` and `hold_paid`, and a hand-edited grid still carrying the solver's `admissions` claim fails `admissions` — the week-term forgeries now run on Fri/Sat and drop the claim, and the exclusive forgery asserts its failure alongside the hold ones. mypy refused `_MODEL_AT` as a dict of `ModelMetaclass` until annotated, and the schedule loop's `sum` over untyped dicts until the counts were kept as plain ints. The demand terms sheet said "no other title" seven times in a row; identical evidence across the days now reads *every day …*.

**For Bruno.** Nothing.

**Still rough.** The week floor is necessary, not sufficient: `day_bound` counts hours and blocks and ignores the other titles' terms, the stagger and the staff, so a week minimum can be carried to Wednesday and found infeasible there where a spread from Thursday would have fit, and the conflict then blames Wednesday; the objective alone decides the spread. `exclusive_until` is inclusive of the named day, one reading of "until". `plf_lock` on the Regent is `exclusive_screen` on Screen 1 by another name, since the house has one PLF room; the term earns its keep on a house with two. A relaxed week term is dropped on one day's grid and reported at week scope, which takes two tables to read, and `relax_key` puts a week term after the same title's day terms without asking whether the week's promise is the bigger one. The terms sheet's `screens` / `min_capacity` rows borrow the day's `eligibility` check, which is not per title. `validation_sentences` names an error inside a day override (`days → Tue → terms → bees`) by the pydantic path, not with the day's own words, and a `ValidationError` raised inside `WeekBrief._check` while unfolding a day surfaces as one long line. `day_bound` enumerates candidates once per film per day — cheap on three screens, to be measured on Day 8. The sheet's verdict column sits far right of a wide "as delivered" column at 1280 px. The old Mon–Wed `exclusive_screen: false` overrides are gone from both examples, so nothing in `examples/` exercises a per-day term override on the opening title any more; the Tuesday `bees min_shows 3` still does.

## Day 4 — 2026-09-10 — The booth's realities

**Shipped.** Five things the booth knows that the model did not. *Credits overlap:* a film's `credits_min` lets the turnaround begin that many minutes before the feature ends; the block shrinks by the same, and the room is clear at the later of feature end and turnaround end, so the next preshow never starts over the last reel (ADR-012). *Preshow by format:* `policy.preshow_by_format` gives 3D or PLF a longer preshow than the house figure (`brief.preshow_for`). *Staff:* `policy.max_concurrent_turnarounds` is how many rooms the floor can clear at once — in the model an optional interval per candidate over its turnaround under `add_cumulative` with the staff as capacity (left out when the cap is not below the screen count, since it could never bind); in the checker a count of open turnarounds at the moment each one begins. *Per-screen hours:* `screen.open` and `screen.last_start` override the house's; candidates, the redundant day-length sum and the `hours` check all read the screen's own; the slot grid stays anchored to the house's opening. *The stagger as a window:* `stagger_min` is now the window and `max_starts_per_window` the count (default 1, so 1-in-10 is what every existing brief still means); the model's sliding sum takes the count, the checker counts every window that begins at a start. And a sixth the playbook implied: a week day may override a screen (`DayOverride.screens`), so Screen 3 can open at noon on weekdays without a second brief. The checker learned each rule first and `tests/test_booth.py` catches a hand-built violation of every one — a clear that ignores the credits, a 3D title given the house preshow, three rooms clearing under two ushers, a start before a screen's own doors, three starts in a 15-minute window that allows two — before the solver is shown to honour it; the property test's brute-force re-read learned all five and its strategies now draw them. The sheet: the orange hatch is pulled left across the feature's tail behind a dashed ink line where the credits roll, the preshow strip is sized per format, a screen with its own hours says so under its name, the legend states the preshows, the stagger rule and the floor's capacity; the hover title says when the turnaround begins over the credits. `explain` names the staff cap in the house sentence. `examples/booth.json` is the Regent with all of it: 11 minutes of PLF credits, 3D 25′ and PLF 30′ preshows, two ushers, Screen 3 opening at noon, at most 2 starts in 15. README learns the booth. 17 new tests; 61 in 29 s.

**Solver.** Booth: `OPTIMAL` · 2.315 s · **1,625.1 expected admissions** of 2,840 seats · 15 sessions · 16 turned away (Harvest, Signal) · every session turns around over its credits · busiest moment 2 rooms clearing at 20:37 · 21 checks green. Screenshot `docs/grids/day-4-booth.png`. Regent (defaults unchanged): `OPTIMAL` · 6.355 s · 1,608.6 · 15 sessions — the same grid value as Day 3, which is the proof the new fields default to what the old model meant. Regent week: all `OPTIMAL` · 27.0 s · 106 sessions · 11,196.0 · Tuesday still pays 120 · 3 of 5 hold. Demand week: all `OPTIMAL` · 17.3 s · 106 sessions · 15,489.6 · 5 of 5 hold. Regent overbooked with `--relax`: `OPTIMAL` · 25.8 s · 1,573.2 · the same 4 terms dropped. The Regent and its week ran while a test run shared the machine; the seconds are contended and Day 8 measures properly.

**Broke and fixed.** Three of the new tests had my arithmetic wrong, not the code's: the third turnaround in the staff test began ten minutes after the other two had ended (y is ten minutes shorter than x, so it starts ten later, not thirty); the credits hand-grid put the second show at 14:12, off the 5-minute grid; the packing test expected ten shows where the stagger offsets the second screen by ten minutes and only nine fit. mypy caught two loop variables in the checker (`t`, then `a`/`b`) re-used with another type further down the same function; renamed. The property brute's first draft of the clear formula was the old one; it now writes the max out longhand rather than calling the model.

**For Bruno.** Nothing.

**Still rough.** The staff cap, per-screen hours and stagger window are house policy, not terms: they are never relaxed and never named in a conflict, so a day that is infeasible because two ushers cannot clear three rooms blames the distributor terms that no longer fit, and the sentence only says "2 rooms clearing at once" beside the house. "At most 2 starts in 15" permits two starts at the same minute; a house that wants both a window and a never-together rule has one stagger and cannot say so. The cumulative costs the proof, not the answer: the booth example solves to the same 1,625.1 in 1.4 s without the two-usher cap and 5.2 s with it (the tiny brief does not notice either way), so on this house the cap never binds the optimum and only makes optimality harder to prove; Day 8 should measure its share on sixteen screens. The credits overlap on the sheet is a dashed line and hatch over cream, legible at Regent scale; Day 6's identity pass should look at it at print size. A screen's own hours push its label to three lines. The `timing` evidence says "15 turn around over the credits" even when a title's credits are shorter than the clean and the clear is unchanged in effect. `DayOverride.screens` is in the model and tested but no example uses it. `run.sh` takes about 85 s.

## Day 3 — 2026-09-09 — Demand

**Shipped.** The placeholder objective is gone (ADR-006 superseded by ADR-011). A title may carry a `demand` block: expected admissions for its *first* session in each daypart on an ordinary day, a `decay` per further session in the same daypart (the second 19:00 show earns less), `weekday` multipliers by day name, a `holiday` multiplier that applies when the day's policy says `school_holiday`, and an `assumptions` list so the file says in words where its numbers came from. A title without a block is not refused: `weight` × daypart weight × the house's `assumed_admissions` (a Policy field, 100 by default) stands in, with the default decay, so every existing brief still solves and still means something. The objective is now Σ min(expected, capacity): seats *sold*, not seats offered. In the model that is one rank literal per (screen, film, daypart, rank) — the rank count is an upper bound on how many sessions of the title can start in that daypart at all — tied to the session booleans by a sum per screen, at most one holder per rank, ranks filling in order; the solver hands out ranks, and because a bigger room can only sell more, the biggest room takes rank 0 and the objective equals the sorted sum exactly. The hold penalty is now in expected admissions (the Day 2 debt), and the grid carries three figures instead of one: `admissions` (the claim), `hold_paid`, and `objective` = admissions − hold_paid. The checker was written first: `admissions()` re-counts seats sold from the brief and the sessions alone, biggest room first within each title × daypart; the `admissions` check holds the solver's claim to within a cent a session (a hand grid with no claim is reported, not failed); the `objective` check holds objective to admissions − hold_paid; the week gains `hold_paid`, which re-derives what every day after the anchor owed and fails a day that paid something else. The report: per title, seats on offer, expected admissions, and expected turned away at capacity; the sheet's by-title table gains Expected and Turned away columns and a day-total row, the meta leads with expected admissions and names the weekday and school holidays, turned away prints in rust only when non-zero, the footer states the objective in admissions and what was paid to hold; the CLI says who comes, who sits, who is turned away and which titles turned them away. `examples/demand.json` is the Regent's school-holiday week with every assumption written beside its number: the PLF event title fills the big room in prime, the family titles are front-loaded to the matinee and lifted 50–70 % by the holidays, the horror lives after 20:45 and ignores school. Eight new tests: `expected` decays and scales; the weekday comes from the date; two shows in one daypart earn less than one in each; raising a title's demand moves it to the bigger room and pushes the light title's guaranteed shows to the small one; capping capacity leaves turned-away non-zero and reported in the checker, the sheet and the CLI; a forged admissions claim fails `admissions`, a forged objective fails `objective`; the hold penalty is paid in admissions and a week that claims it was not paid fails `hold_paid`; a forecast for a daypart the house does not define is refused in a sentence. The random-brief property now attaches a stated demand block to about half its titles, a random weekday and a random holiday flag, so Hypothesis proves the rank formulation agrees with the checker's sorted re-count across the space, not just on the Regent. 44 tests.

**Solver.** Demand week: 7 days all `OPTIMAL` · 20.2 s in all · 106 sessions · **15,490 expected admissions** of 19,892 seats on offer · 2,206 turned away at capacity (Harvest 843, Bees 902, Voyage 417) · 5 of 5 titles hold Thu/Mon/Tue/Wed · 0 paid to hold. Per day: Thu 4.40 s · 2,089.5, Fri 5.95 s · 2,349.0, Sat 4.34 s · 2,590.0, Sun 2.32 s · 2,192.6, Mon 1.58 s · 2,089.5, Tue 0.65 s · 2,089.5, Wed 0.94 s · 2,089.5. Regent (weights standing in): `OPTIMAL` · 4.449 s · 1,608.6 · 15 sessions · 14 turned away (Dead Signal on Screen 3) · 20 checks green. Regent week: all `OPTIMAL` · 21.7 s · 106 sessions · 11,196.0 · Tuesday pays 120 to hold (Harvest and Bees move for Bees' third show) · 3 of 5 hold. Regent overbooked with `--relax`: `OPTIMAL` · 21.0 s · 1,573.2 · 15 sessions · the same 4 terms dropped. Screenshot `docs/grids/day-3-demand.png`.

**Broke and fixed.** The forged-hold test from Day 2 now fails two week checks, not one: a Tuesday whose starts were moved by hand also paid nothing to hold, and `hold_paid` says so; the test now expects both. The first demand test asked the heavy title to own the big room outright; with 400 admissions in every daypart it also takes the small room whenever the big one is busy, which is right, so the test now asserts the big room is entirely its and the light title's guaranteed shows land in the small one. The forged-claim hand grid had one show of a title that wanted two and failed `min_shows` before it could fail `admissions`. The week test helper dumped the new `weekday` field into a `WeekBrief`, which forbids it. The header meta wrapped "2,206 / turned away at capacity" across two lines; turned away now has a line of its own in all three metas.

**For Bruno.** Nothing.

**Still rough.** The Regent takes 4.4 s where it took 0.8 s: the rank literals and their linking sums are the cost, and the rank count per (title, daypart) is an upper bound that could be tightened by the whole-house stagger; Day 8 will feel this on sixteen screens. "Biggest room takes rank 0" is the optimistic reading of a demand model that does not say which physical session draws the first audience; it is written down in ADR-011 but not on the sheet. `assumed_admissions` defaults to 100 in the code, which is the kind of number the playbook wanted in the file; it is at least a Policy field the house can state. A stated demand block that omits a daypart makes sessions there worth nothing, so the solver will not place one unless a term forces it, and a start outside every daypart draws nothing for a stated block. The overbooked Regent still drops Atlas to zero shows once its minimum is relaxed: the demand objective gave it no reason to keep a 90-admission arthouse show when a sixth Harvest matinee fits the same hour on the same screen; Day 1's suggestion that a dropped minimum should survive as a soft goal stands. The by-title table speaks two vocabularies in one column ("w2.2" for a weight, "demand stated" for a block). The week header wraps "school holidays" onto its own line. `run.sh` now takes about 75 s (two weeks at 20 s each, the relaxed Regent at 21 s).

## Day 2 — 2026-09-08 — The week

**Shipped.** The week as a unit. A `WeekBrief` is one house, one slate, one base policy and up to seven `DayOverride`s, each a name, a date, and only what differs: policy fields (`last_start 22:30` on Friday and Saturday, `open 11:00` on Sunday) and per-film term fields (the opening exclusive lifts on Monday; cheap Tuesday gives the family title a third show). `week.day(i)` unfolds to a plain `Brief`, so the solver and the checker never learn what a week is, and every existing term, conflict and relaxation path works unchanged per day (ADR-009). `solve_week` runs day by day into a `WeekGrid` of seven `Grid`s; an infeasible day keeps its own conflict and the rest still solve; `--relax` stays per day. The soft term: the first hold day that solves is the anchor, and every later hold day pays `hold_penalty` weighted seats (60 by default) per title whose *set of start times* differs from the anchor's — one `differs` literal per title, forced on if any anchor start is not even on offer that day, so nothing is ever forced and a title that cannot hold simply pays. `WeekGrid.held` lists the titles that kept their starts across every solved hold day. The checker (`check_week`, no solver code) re-checks every day against its unfolded brief, then the week's own claims: the grids line up with the days, and the `held` list is exactly what an independent re-read of the start sets finds; a forged claim fails. The CLI tells a week brief from a day brief by its `days` key, so `plan` / `check` / `render` / `validate` all take either. The sheet gains the week view: the day grid and tables became Jinja macros shared by both sheets; the week sheet opens with the by-title table across seven days (hold days washed in navy, a *HOLDS* stamp in blue — the orange stays on the turnaround), the week's proof, then every day on its own page with a print page-break. `examples/regent-week.json` is the Regent's Thu–Wed. Hypothesis arrives: 25 random small briefs through the solver always check clean, and 300 random hand-built grids get the same verdict from the checker and an all-pairs brute-force re-read written from the brief in the test. Nine week tests besides. 36 tests in 18 s.

**Solver.** Regent week: 7 days all `OPTIMAL` · 7.3 s in all · 106 sessions · 26,094 weighted seats · 3 of 5 titles hold their times Thu/Mon/Tue/Wed (Voyage, Signal, Atlas; Harvest breaks only on Tuesday when Bees takes its third show, Bees breaks with it). Per day: Thu 0.99 s · 3741, Fri 0.96 s · 3880, Sat 0.99 s · 3880, Sun 0.90 s · 3557, Mon 0.51 s · 3741, Tue 2.45 s · 3555, Wed 0.50 s · 3741. Regent: `OPTIMAL` · 0.818 s · 3741 · 15 sessions, 18 checks green. Regent overbooked with `--relax`: `OPTIMAL` · 21.6 s · 3797.62 · 4 terms dropped, as yesterday. Screenshot `docs/grids/day-2-week.png`.

**Broke and fixed.** `.venv` went hidden again after `uv add` and `import turnaround` vanished mid-session; the scripts already clear the flag, the interactive shell did not. The first hold test assumed Monday would give the tiny brief's heavy title two shows and Tuesday's `min_shows 3` would break it; Monday already gives three. Rewritten as a `max_shows 1` on Tuesday, which then showed the soft term doing exactly what it should: with a 60-seat penalty the *other* title also moved, because the freed screen time was worth more than the penalty. The test now sets a penalty no seat gain can pay for, so the untouched title must hold and the capped one must still break — a hard term beats any penalty. A `by_start` name was reused in `_build` and mypy caught it before the tests could.

**For Bruno.** Nothing.

**Still rough.** The hold penalty is in weighted seats, the placeholder unit of ADR-006; Day 3 has to restate it in admissions or the trade-off means nothing. The anchor is the first hold day in week order, which for a Thursday-opening week is the day that still carries the exclusive, so Mon–Wed are pulled toward Thursday's grid rather than the one the three of them would agree on. Hold compares start sets only, not rooms: a title can keep its times and change screens, which is probably right (customers remember times) but is not written down anywhere the programmer would see. The committed screenshot is the top 1100 px: the week table and its proof; the per-day pages are below the fold and the print stacking is untested against `--print-to-pdf` until Day 6. The week's proof table sits alone in the left column with nothing on the right. The random-brief property skips `INFEASIBLE` and `UNKNOWN` briefs rather than asserting anything about them, and its slot grid is 15 or 30 minutes to keep 25 examples under ten seconds. `_is_week` parses the JSON once to peek and pydantic parses it again.

## Day 1 — 2026-09-07 — Infeasibility, explained

**Shipped.** Every distributor term now sits behind its own assumption literal in the model, one `TermRef` per (film, term) for `min_shows`, `max_shows`, `prime_shows` and `exclusive_screen`. On `INFEASIBLE` the solver first probes each term alone (a term that fails by itself is a conflict of size one and is reported as such), otherwise takes the unsat core from `sufficient_assumptions_for_infeasibility()` and shrinks it by deletion so nothing is blamed that the contradiction does not need. `plan` exits 2 and speaks the trade's words: *"this cannot hold on its own: The Long Voyage min_shows 6 — the house has 3 screens (1 PLF), doors 10:00 to last start 21:30 and a 17:30–20:45 prime window."* `plan --relax` drops terms from the conflict one at a time (exclusive last, lightest film first, prime before max before min) until a grid exists; the grid carries `relaxed`, the CLI prints each drop, and the sheet stamps *RELAXED* in rust beside the failing check with "4 terms given up · see proof" in the header. The checker (still no solver code, ADR-003) accepts a failure only where the grid declares it, and fails a declared relaxation that names a film or term the brief does not carry; `Report.ok` tolerates declared relaxations, `Report.clean` does not. `examples/regent-overbooked.json` (minimums doubled, Harvest given a prime guarantee) is the infeasible showcase; `run.sh` re-plans it with `--relax` when plain `plan` exits 2. Nine new tests: three constructed conflicts, each confirmed minimal by the solver itself; the relax order; the doubled Regent relaxed to a checkable grid; declared, undeclared and bogus relaxations through the checker.

**Solver.** Regent: `OPTIMAL` · 1.639 s · 3741.0 · 15 sessions, 18 checks green. Regent overbooked, strict: `INFEASIBLE` in 2.2 s, one culprit named. Regent overbooked with `--relax`: `OPTIMAL` · 24.016 s · 3797.62 · 15 sessions · 4 terms dropped (Voyage min 6, Atlas min 4, Atlas prime 1, Bees min 4); 19 checks, the 4 declared ones ✗ *relaxed*, the rest green. Screenshot `docs/grids/day-1-relaxed.png`.

**Broke and fixed.** The 08:59 slot hit the session limit and did nothing; the 12:46 rerun did the day. The solver, checker, CLI and template changes were already in the working tree, uncommitted, from an interactive session at 11:05–11:17; they were verified, tested and committed as they stood. `import turnaround` then vanished mid-session: Finder flags `.venv` under `~/Documents` as hidden, files uv writes there inherit the flag, and Python 3.13 skips hidden `.pth` files; the scripts now run `chflags -R nohidden .venv` after `uv sync`. `run.sh`'s relax fallback never fired because `$?` after `if ! cmd` is the negated status; it now captures the code with `|| rc=$?`. The first two-exclusives test hard-coded one of three equally minimal conflicts; the tests now ask the solver to confirm minimality instead of guessing which pair it will name. The doubled Regent at 5-minute slots needs 11 s to find its last relaxed grid and came back `UNKNOWN` under a 10 s cap; the test uses 10-minute slots, and the CLI now lists what had already been dropped when time runs out.

**For Bruno.** Nothing.

**Still rough.** Once a `min_shows` is dropped the title can vanish: Atlas and The Bee Kingdom get 0 shows in the relaxed grid because the placeholder objective (ADR-006) has no reason to keep them. A dropped minimum should probably survive as a soft goal; Day 3's demand objective is the honest fix. Relaxation is slow: every round rebuilds the model and probes each term alone, so the overbooked Regent takes 24 s against 1.6 s for the clean one; the alone-probes could be skipped after the first round and the core reused. With several equally minimal conflicts, the order rule alone decides which title pays. The unsat-core solve is single-threaded by construction; the pinned probes run parallel. The fast "too many shows for the hours" proof leans on the redundant per-screen day-length sum in the model.

## Day 0 — 2026-09-06 — The grid exists

**Shipped.** The brief as Pydantic models (`model.py`): screens with capacity, formats and a clean-time override; films with runtime, format, weight, optional daypart multipliers and distributor `Terms` (min/max shows, prime shows, exclusive screen, start window, screen and capacity restrictions); a `Policy` for hours, preshow, turnaround, stagger, slot size and dayparts. The CP-SAT model (`solve.py`): one boolean per (screen, film, start), an optional interval per boolean sized `preshow + runtime + clean`, `NoOverlap` per screen, sliding-window stagger house-wide, every term as a linear constraint, exclusive screens via a per-screen indicator that forbids every other title there. Objective: weighted seats on offer (ADR-006, a placeholder). The independent checker (`check.py`) with a proof table. CLI `plan` / `check` / `render` / `validate`. The week sheet (`render.py` + `templates/sheet.html.j2`). Sixteen tests. CI on Ubuntu. `scripts/build.sh`, `test.sh`, `run.sh`, `slot.sh`.

**Solver.** The Regent (3 screens · 5 titles · one PLF exclusive with a prime guarantee · a kids' 3D title capped at 17:00 · a horror title held to 16:00 or later): `OPTIMAL` in 0.957 s, 15 sessions, all 17 checks green. The grid reads right: the exclusive owns Screen 1 with four shows including 17:30; the family title takes 14:50 and 16:55 on the only 3D room; the horror closes both smaller rooms; no two starts within 10 minutes.

**Broke and fixed.** OR-Tools 9.15 removed the CamelCase API from its type stubs and `objective_value` became a property; the model was written against the old names and mypy caught all thirteen before the tests did. The tests then caught the property.

**For Bruno.** Nothing today. The launchd agent, the skill, the Linear project and the GitHub repository were created in the same session.

**Still rough.** The objective is judgment-weighted seats, not admissions (Day 3). `INFEASIBLE` is a verdict without a reason (Day 1). The sheet's identity pass is a first draft (Day 6). The infeasible-terms test spends its full 10 s limit proving nothing can work; a smaller limit or an early cut would make the suite faster.
